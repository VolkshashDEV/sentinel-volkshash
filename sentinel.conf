# specify path to volkshash.conf or leave blank
# default is the same as VolkshashCore
#volkshash_conf=/home/evan82/.volkshashcore/volkshash.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
